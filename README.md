##  🚀 Идея проекта: "URL Shortener Service"
###  Это пример - просто для понимания, но может быть очень нагруженным. 

### Что будем делать:
- Сокращение длинных URL
- Редирект по коротким ссылкам
- Статистика переходов
- TTL для ссылок (автоудаление старых)

### 🛠 Технологический стек:
- Kotlin + Spring Boot 3
- Gradle (Kotlin DSL)
- PostgreSQL - основные данные
- Redis - кэш для редиректов
- Kafka - асинхронная обработка статистики
- Docker + Docker Compose - для запуска инфраструктуры

### 🎯 Что мы получим:
- PostgreSQL - надежное хранение данных
- Redis - быстрые редиректы (тысячи RPS)
- Kafka - асинхронная обработка кликов без блокировки
- Docker - изолированное окружение
- Kotlin + Gradle - современная разработка

### 🔍 Почему именно так можно протестировать соединение с БД, Кафкой, Редисом:
- `Spring Boot` сканирует компоненты рекурсивно начиная от пакета главного класса
- `@Component` аннотация автоматически подхватит класс при сканировании
- `ApplicationRunner` интерфейс гарантирует выполнение кода после запуска приложения
- Вариант с ApplicationRunner предпочтительнее для тестов подключения, так как гарантирует выполнение после полной инициализации контекста Spring


### Механизму автоматической генерации идентификаторов в JPA/Hibernate

### 🔧 Как это работает:

### **1. Аннотация @GeneratedValue**

В вашей сущности `ShortUrl`:

```kotlin
@Entity
@Table(name = "short_urls")
data class ShortUrl(
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)  // ← Вот это ключевая аннотация!
    val id: Long = 0,
    // ... остальные поля
)
```

### **2. Стратегия GenerationType.IDENTITY**

`GenerationType.IDENTITY` означает, что база данных сама генерирует уникальные ID через **auto-increment** механизм.

**Что происходит в PostgreSQL:**
- При создании таблицы Hibernate автоматически создает столбец с `SERIAL` или `BIGSERIAL` типом
- PostgreSQL использует **sequence** (последовательность) для генерации чисел
- Каждая новая запись получает значение `nextval('sequence_name')`

### **3. Можно посмотреть созданную sequence:**

Подключитесь к БД и выполните:
```sql
-- Посмотреть sequences в вашей БД
SELECT sequence_name FROM information_schema.sequences 
WHERE sequence_name LIKE '%short_urls%';

-- Обычно создается sequence с именем: short_urls_id_seq
-- Текущее значение sequence
SELECT nextval('short_urls_id_seq');
```

## 🎯 Альтернативные стратегии генерации ID:

### **A. GenerationType.SEQUENCE (для продвинутых сценариев)**
```kotlin
@Id
@GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "short_url_seq")
@SequenceGenerator(name = "short_url_seq", sequenceName = "short_url_id_seq", allocationSize = 1)
val id: Long = 0
```

### **B. GenerationType.TABLE (менее эффективно)**
```kotlin
@Id
@GeneratedValue(strategy = GenerationType.TABLE, generator = "short_url_generator")
@TableGenerator(name = "short_url_generator", table = "id_generator", pkColumnName = "entity_name", valueColumnName = "next_id")
val id: Long = 0
```

### **C. UUID (для распределенных систем)**
```kotlin
@Id
@GeneratedValue(generator = "UUID")
@GenericGenerator(name = "UUID", strategy = "org.hibernate.id.UUIDGenerator")
val id: UUID? = null
```

## 🔍 **Что происходит при сохранении:**

```kotlin
// 1. Создаем объект БЕЗ ID (id = 0)
val shortUrl = ShortUrl(
    shortCode = "abc123", 
    originalUrl = "https://example.com"
)

// 2. Сохраняем
val savedUrl = shortUrlRepository.save(shortUrl)

// 3. Hibernate выполняет:
//    - INSERT INTO short_urls (short_code, original_url, ...) 
//      VALUES ('abc123', 'https://example.com', ...)
//    - PostgreSQL возвращает сгенерированный ID
//    - Hibernate устанавливает этот ID в объект

println(savedUrl.id) // → 1, 2, 3, ... (автоматически)
```

## 💡 **Преимущества IDENTITY стратегии:**

- **Простота** - не требует дополнительной конфигурации
- **Эффективность** - генерация на уровне БД
- **Гарантированная уникальность** - даже в конкурентной среде
- **Порядок следования** - ID всегда увеличиваются на 1

## ⚠️ **Особенности работы:**

- **Не batch-able** - нельзя использовать пакетную вставку
- **ID известен только после коммита** - при откате транзакции ID может "пропасть"
- **Для высоконагруженных систем** иногда лучше использовать UUID или sequence с allocationSize

## 🚀 **Проверка в вашем проекте:**

Вы можете добавить логирование, чтобы увидеть процесс:

```kotlin
@Service
class UrlShorteningService(
    private val shortUrlRepository: ShortUrlRepository
) {
    
    fun createShortUrl(originalUrl: String): ShortUrl {
        val shortUrl = ShortUrl(
            shortCode = generateShortCode(),
            originalUrl = originalUrl
        )
        
        println("📝 Before save - ID: ${shortUrl.id}") // → 0
        
        val savedUrl = shortUrlRepository.save(shortUrl)
        
        println("💾 After save - ID: ${savedUrl.id}") // → 1, 2, 3, ...
        
        return savedUrl
    }
}
```

**Автоматический и надежный механизм**, который избавляет нас от ручного управления идентификаторами!

---

### Вот несколько способов посмотреть работу с Redis:

### 📊 **Команды для ручной проверки Redis**

Подключитесь к Redis CLI:

```bash
# Зайдите в контейнер Redis
docker exec -it ваш_контейнер_redis redis-cli

# Или подключитесь с хоста
redis-cli -h localhost -p 6379
```

**Полезные команды:**
```bash
# Посмотреть все ключи
KEYS *

# Посмотреть ключи url:*
KEYS url:*

# Получить значение по ключу
GET url:abc123

# Узнать TTL ключа (сколько секунд осталось жить)
TTL url:abc123

# Посмотреть информацию о Redis
INFO memory
INFO stats

# Мониторить все операции в реальном времени
MONITOR
```

### 🚀 **Протестируем работу Redis**

После добавления логирования:

1. **Создайте короткий URL:**
```bash
   POST http://localhost:8080/shorten
   {"url": "https://example.com"}
```

2. **В логах увидите:**
```
   💾 Saving to Redis - key: url:abc123, value: https://example.com, TTL: 1 hour
   ✅ Successfully saved to Redis
```

3. **Выполните редирект:**
```bash
   GET http://localhost:8080/abc123
```

4. **В логах увидите:**
```
   🔍 Looking up short code: abc123
   🔎 Checking Redis cache for key: url:abc123
   🎯 Redis HIT - Found in cache: https://example.com
```

5. **Проверьте статистику:**
```bash
   GET http://localhost:8080/api/redis/stats
```

## 💡 **Что мы увидим:**

- **Cache Hit** - когда URL берется из Redis (быстро)
- **Cache Miss** - когда URL не найден в Redis и идет в БД
- **Cache Update** - когда URL сохраняется в Redis после запроса к БД
- **Статистику** - сколько URL в кэше, TTL и т.д.

---

### Несколько отличных UI для Redis:

### 🎯 **RedisInsight (официальный от Redis)**

```yaml
version: '3.8'
services:
  
  redis-insight:
    image: docker.io/redis/redisinsight:latest
    ports:
      - "5540:5540"
    volumes:
      - redis_insight_data:/db
    depends_on:
      - redis

volumes:
  redis_insight_data:
  postgres_data:
```

## 🎯 **Redis Commander (легковесный)**

```yaml
redis-commander:
  image: docker.io/rediscommander/redis-commander:latest
  environment:
    - REDIS_HOSTS=local:redis:6379
    - HTTP_USER=admin
    - HTTP_PASSWORD=password
  ports:
    - 8081:8081
  depends_on:
    - redis
```

## 🔍 **Доступ к UI:**

### **RedisInsight** (рекомендуется)
- **URL:** http://localhost:5540
- **Особенности:** Официальный, много функций, мониторинг, анализ

### ⚙️ **Настройка подключения в RedisInsight:**

1. Откройте http://localhost:5540
2. Нажмите "Add Redis Database"
3. Заполните:
    - **Host:** redis
    - **Port:** 6379
    - **Name:** url-shortener-cache

### **Redis Commander**
- **URL:** http://localhost:8081
- **Особенности:** Легковесный, простой интерфейс

### 💡 **Что вы увидите в RedisInsight:**

- **Все ключи** с префиксом `url:`
- **TTL** для каждого ключа
- **Значения** URL
- **Статистику** использования памяти
- **Команды** в реальном времени
- **Графики** производительности

Теперь вы сможете в реальном времени наблюдать как:
- Ключи добавляются при создании коротких URL
- Ключи читаются при редиректах
- TTL уменьшается
- Кэш автоматически очищается

Рекомендую **RedisInsight** - он самый функциональный и удобный для нашего проекта!

---
